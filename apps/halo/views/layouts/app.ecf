<!DOCTYPE html>
<html lang="en">
@include("partials.header", { pageTitle: title ?? "Halo App" })
<body>
    @include("partials.navbar")

    <main>
        @yield('content')
    </main>

    @include("partials.footer")
</body>
</html>
