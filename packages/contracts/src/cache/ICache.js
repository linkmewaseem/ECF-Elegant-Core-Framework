export class ICacheManager {
  store(name = null) { throw new Error("Contract interface method."); }
  get(key) { throw new Error("Contract interface method."); }
  put(key, value, ttl) { throw new Error("Contract interface method."); }
}
