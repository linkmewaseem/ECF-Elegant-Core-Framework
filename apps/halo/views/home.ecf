@extends('layouts.app')

    @section('content' )

        <h1 class="header-title">Welcome, {{ name }}!</h1>
        <p>Let's explore the features and capabilities of ECF View Engine.</p>

        <x-alert type="success" title="System Status:">
        All View Engine systems operational including Component Engine!
        </x-alert>

        <x-card class="shadow-sm">
        <x-slot:header>
        <h3>Account Summary</h3>
        </x-slot:header>

        <p><strong>Name:</strong> {{ name }}</p>
        <p><strong>Age Status:</strong> {{ age >= 18 ? 'Adult (' + age + ')' : 'Minor (' + age + ')' }}</p>
        <p><strong>Server Date:</strong> {{ date }}</p>
        </x-card>

        @for(users as user, i)
            @continue(user.disabled)
            @include("partials.user-row", { user: user })
        @endfor
    @endsection

