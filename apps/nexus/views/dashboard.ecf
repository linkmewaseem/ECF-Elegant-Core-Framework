@extends('layouts.app')

@section('content')
    <div class="grid-stats">
    <div class="stat-card">
    <span class="stat-label">Total Inventory Items</span>
    <span class="stat-val">{{ totalProducts }}</span>
    </div>
    <div class="stat-card">
    <span class="stat-label">Total Stock Value</span>
    <span class="stat-val">${{ totalStockValue }}</span>
    </div>
    <div class="stat-card">
    <span class="stat-label">Low Stock Alerts</span>
    <span class="stat-val" style="color: var(--danger);">{{ lowStockCount }}</span>
    </div>
    <div class="stat-card">
    <span class="stat-label">Active Database Engine</span>
    <span class="stat-val" style="font-size: 1.3rem; color: var(--primary);">SQLite (ECF DB)</span>
    </div>
    </div>

    <div class="glass-card">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
    <h2>Top Inventory Products</h2>
    <a href="/products/new" class="btn btn-primary">+ New Item</a>
    </div>

    <table class="data-table">
    <thead>
    <tr>
    <th>ID</th>
    <th>Product Name</th>
    <th>Category</th>
    <th>Price</th>
    <th>Stock Level</th>
    <th>Status</th>
    <th>Action</th>
    </tr>
    </thead>
    <tbody>
    @for(products as item)
        <tr>
        <td>#{{ item.id }}</td>
        <td><strong>{{ item.name }}</strong></td>
        <td>{{ item.category }}</td>
        <td>${{ item.price }}</td>
        <td>{{ item.stock }}</td>
        <td>
        @if(item.stock <= 10)
            <span class="badge badge-danger">Low Stock</span>
        @else
            <span class="badge badge-success">In Stock</span>
        @endif
        </td>
        <td>
        <a href="/products/{{ item.id }}" style="color: var(--primary); text-decoration: none;">View</a>
        </td>
        </tr>
    @endfor
    </tbody>
    </table>
    </div>
@endsection