export class INotificationManager {
  send(notifiables, notification) { throw new Error("Contract interface method."); }
  sendNow(notifiables, notification, channels) { throw new Error("Contract interface method."); }
}
