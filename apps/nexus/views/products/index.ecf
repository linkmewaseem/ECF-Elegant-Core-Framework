@extends('layouts.app')
@section('content')
    <div class="glass-card">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
    <div>
    <h2>Inventory Catalog</h2>
    <p style="color: var(--text-muted); font-size: 0.9rem;">Queried via <code>DB.table("products").get()</code></p>
    </div>
    <a href="/products/new" class="btn btn-primary">+ Add New Product</a>
    </div>

    <table class="data-table">
    <thead>
    <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Category</th>
    <th>Price</th>
    <th>Stock</th>
    <th>Created At</th>
    <th>Action</th>
    </tr>
    </thead>
    <tbody>
    @for(products as item)
        <tr>
        <td>#{{ item.id }}</td>
        <td><strong>{{ item.name }}</strong></td>
        <td>{{ item.category }}</td>
        <td>${{ item.price }}</td>
        <td>{{ item.stock }}</td>
        <td style="color: var(--text-muted);">{{ item.created_at }}</td>
        <td>
        <a href="/products/{{ item.id }}" style="color: var(--primary); text-decoration: none;">View</a>
        </td>
        </tr>
    @endfor
    </tbody>
    </table>
    </div>
@endsection
