export class IQueueManager {
  push(job, data, queue) { throw new Error("Contract interface method."); }
  later(delayInSeconds, job, data, queue) { throw new Error("Contract interface method."); }
}
