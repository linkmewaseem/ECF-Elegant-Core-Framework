import { ServiceProvider } from '../../../../../core/src/index.js';

export class AppServiceProvider extends ServiceProvider {
  register() {
    // Register container bindings
  }

  boot() {
    // Boot application services
  }
}

export default AppServiceProvider;
