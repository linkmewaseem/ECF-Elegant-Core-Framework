# Soft Deletes Extension Architecture

`@ecfjs/soft-deletes` is an ORM extension. Its public entry point is `index.js`; `SoftDeletesPlugin.js` contains the plugin implementation. It depends on the host ECF database integration and does not create an application runtime by itself.
